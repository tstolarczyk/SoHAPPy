# Conda environment for SoHAPPy version roma
# Install:    conda env create -f SoHAPPy-roma.yml
# Activate:   conda activate roma
# Deactivate: conda deactivate

name: roma


dependencies:
  - gammapy=0.12
  - matplotlib=3
  - pandas